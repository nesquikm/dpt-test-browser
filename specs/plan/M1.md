---
milestone: M1
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
kind: scaffolding
---

# Implementation Plan

## Milestone Order

Each milestone gets its own `specs/plan/M<N>.md` file. Don't proceed to M<N+1> until M<N> gates pass.

### Task Sizing

Each task ≈ one commit's worth of work. Two-line entries: action + `verify:`.

## M1: Bootstrap & Browser MVP {#M1}

**Goal:** Project scaffold lands at HEAD; a single-tab browser shell renders an embedded webview on macOS / iOS / Android with working back / forward / reload + URL bar.

**Prerequisites:** None.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| <scaffolding> | Bootstrap (Flutter scaffold + dev-process-toolkit shipped pre-toolkit) | n/a |
| STE-254 | Tab model + TabManagerCubit | linear:`STE-254` |
| <tracker-id> | Webview adapter interface + `webview_flutter` impl | linear:`<tracker-id>` |
| <tracker-id> | Browser shell UI (tab bar + address bar + active tab view) | linear:`<tracker-id>` |
| <tracker-id> | Multi-tab support (open, close, switch) | linear:`<tracker-id>` |

**Tasks:**
- [x] Scaffold Flutter project (macOS / iOS / Android)
  verify: `fvm flutter analyze && fvm flutter test` clean
- [x] Bootstrap dev-process-toolkit (CLAUDE.md, settings, specs, hooks)
  verify: `git log -1 --format=%s` shows `chore: bootstrap dev-process-toolkit`
- [x] Decide webview package (ADR in technical-spec.md)
  verify: ADR table row in technical-spec.md filled in (`webview_flutter` ^4.13.1)
- [ ] Add `webview_flutter` ^4.13.1 + minimal "loads google.com" smoke
  verify: `fvm flutter run -d macos` displays google.com homepage
- [ ] Align platform deployment targets (iOS 13+, Android `minSdkVersion 24`, macOS 10.15+)
  verify: `fvm flutter analyze` clean after edits to Podfile / build.gradle.kts / AppInfo.xcconfig
- [x] Define `BrowserTab` model (Freezed) + `TabManagerCubit` skeleton
  verify: `fvm flutter test test/tabs/` passes; cubit unit tests cover open/close/select
- [ ] Build `BrowserShell` widget (tab strip + address bar + active tab webview)
  verify: widget test renders shell with one tab and pumps a URL change
- [ ] Wire address bar → cubit → webview navigate
  verify: integration of address-bar input round-trips through cubit to fake adapter (test)
- [ ] Multi-tab: open / close / switch tabs
  verify: cubit tests for 2-tab open / close / select sequences; widget test for tab strip click

**Tests:**
- Cubit state-transition tests for `TabManagerCubit`.
- Widget tests for `TabBar`, `AddressBar`, `BrowserShell`.
- URL parser unit tests in `lib/shared/url.dart`.

**Acceptance Criteria:**
- [ ] `fvm flutter analyze` clean.
- [ ] `fvm flutter test` all green.
- [ ] App runs on macOS, iOS simulator, Android emulator and loads a real URL.
- [ ] Tabs can be opened, closed, switched without crashing.

**Gate:** `fvm flutter analyze && fvm flutter test`
