---
milestone: M2
status: archived
archived_at: 2026-05-08T14:53:06Z
kickoff_branch: null
frozen_at: null
kind: feature
---

# Implementation Plan — M2

## M2: Tab keep-alive + state persistence {#M2}

**Goal:** Tabs survive switching with full state preservation; foundation for tab/URL persistence across app launches.

**Prerequisites:** M1 complete (archived).

**FR list:**

| FR      | Title                                          | Tracker            |
|---------|------------------------------------------------|--------------------|
| STE-260 | Tab webview keep-alive across tab switches     | linear:`STE-260`   |

**Tasks:**
- [x] Refactor `_ShellBody` to mount all tabs via `IndexedStack`-or-equivalent keep-alive layout — implementation chose the FR-Notes-authorised `Stack + keyed Visibility.maintain` fallback (deviation classification: `contradicts`, resolved per Notes; see STE-260 Notes for rationale). (FR: STE-260)
  verify: `fvm flutter test test/ui/browser_shell_test.dart` passes the new keep-alive group
- [x] Hoist adapter-sink publication out of `BrowserTabView` into `BrowserShell` state via `onAdapterReady(tabId, adapter)` (FR: STE-260)
  verify: address-bar widget test asserts post-switch adapter binding
- [ ] Manual platform smoke (macOS / iOS sim / Android emu) — fill Notes table in STE-260 (FR: STE-260)
  verify: each platform row in the FR's Notes is filled with `pass` or `fail + artifact`

**Tests:**
- New `'IndexedStack keep-alive'` group in `test/ui/browser_shell_test.dart` (3-tab keep-alive, dispose isolation, address-bar sink binding).
- Extension to `test/ui/address_bar_test.dart` for post-switch adapter rebinding.

**Acceptance Criteria:**
- [x] All ACs of STE-260 pass (AC.7 manual smoke deferred — Notes table marked TBD pending platform-device runs; AC.8 gate clean).
- [x] Gate `fvm flutter analyze && fvm flutter test` clean.

**Gate:** `fvm flutter analyze && fvm flutter test`
